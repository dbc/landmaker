=========
landmaker
=========

landmaker is a plug-in driven footprint generator for pcb layout tools.
It creates footprints for the ``pcb`` tool in gEDA, but is CAD system
agnostic so back-end renderers can target any footprint file format.
